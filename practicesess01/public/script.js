function showAlert(message){
    const alertBox = document.getElementById('conferenceAlert');
    alertBox.innerText = message;
    alertBox.style.color ="green";
    alertBox.style.fontweight = "bold";   
}

